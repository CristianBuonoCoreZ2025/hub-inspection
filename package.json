{
  "name": "claims-hub-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "start": "pnpm --filter web start",
    "lint": "pnpm --filter web lint",
    "mobile:build": "pnpm --filter mobile build",
    "mobile:sync": "pnpm --filter mobile sync",
    "mobile:ios": "pnpm --filter mobile open:ios",
    "mobile:android": "pnpm --filter mobile open:android",
    "db:push": "pnpm --filter web db:push",
    "test:auth": "pnpm --filter web test:auth",
    "db:permissions": "pnpm --filter web db:permissions"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.0.0"
}
