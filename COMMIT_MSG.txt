fix: eliminar todas las queries anidadas de Supabase (!inner con .eq en relacion)

Revisados TODOS los comboboxes y queries de la app. Encontradas y corregidas
3 funciones mas que usaban el patron roto:

- checkPrerequisiteGestion: 2 queries separadas (template por codigo → claim_actions)
- checkGestionExists: 2 queries separadas (template por codigo → claim_actions)
- getInspectorSchedule: 2 queries separadas (claims del inspector → sesiones)
- inspection/live route: quitado !inner innecesario en claims_participants

Patron correcto: queries separadas + join/filtro client-side
Patron roto: .eq("relacion.campo", valor) no filtra en Supabase

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
