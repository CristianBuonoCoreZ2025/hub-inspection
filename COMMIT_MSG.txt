fix: reescribir queries cascading sin relaciones anidadas de Supabase

Las queries anidadas de Supabase (!inner con .eq en relacion) no funcionan
correctamente. Reescritas como queries separadas + join client-side.

- fetchTemplatesWithLine: 3 queries separadas (atcs → templates → business_lines)
- getActionTemplatesByClaimStatus: 2 queries separadas (atcs → templates)
- Esto arregla TODOS los comboboxes en cascada del workflow

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
