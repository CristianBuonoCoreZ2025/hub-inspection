fix: dependencias por codigo (no template ID) + glassmorphism

Problema: la migracion 139 hizo CROSS JOIN creando 16 dependencias
duplicadas (4 COB x 4 RES = 16 pares COB->RES).

Solucion: cambiar el modelo a dependencias por CODIGO, no por template ID.
Los paquetes son globales (COB->RES->PCA) pero al crear la gestion,
el trigger busca el template hijo por codigo + business_line del claim.

Cambios:
- Migration 142: tabla action_template_dependencies usa parent_code/child_code
- Migration 143: trigger cascade reescrito para buscar por code + business_line
- Servicio template-dependencies.ts: usa codes en lugar de IDs
- Servicio workflow-configs.ts: createWorkflowStepWithChain busca por code + line
- Pagina dependencias: rediseñada con glassmorphism, cadenas visuales
- AGENTS.md: glassmorphism documentado como regla de app

Antes: 16 dependencias duplicadas (4x4 por linea de negocio)
Ahora: 4 dependencias limpias (COB->RES, RES->PCA, NSA->RTA, COI->INS)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
