fix: unificar toda la tipografía del dashboard a 11px y limpiar lint

Se reemplazó `text-[12px]` por `text-[11px]` en todo `src/` (.tsx, .ts, .css,
.cjs) y se actualizaron las utilidades `app-data-grid`, `app-data-grid-2` y
`app-data-grid-4` en `components.css` para usar 11px. De este modo títulos,
labels y valores quedan al mismo tamaño en todas las pantallas.

Además se aprovechó para arreglar los errores/warnings de ESLint que impedían
un build limpio:
- Imports no usados en rutas API y servicios.
- `any` explícitos en `document-render.ts` y `voice-textarea.tsx`.
- Llamada síncrona a `setState` dentro de un `useEffect` en `voice-textarea.tsx`.
- `form.watch()` de react-hook-form en `claims/page.tsx` centralizado en una
  única suscripción reactiva con `eslint-disable-next-line` documentado.

`npx tsc --noEmit` y `npx eslint .` pasan con 0 errores/warnings.

Generado con [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
