feat: umbral de geolocalización configurable desde Configuración

PROBLEMA: el umbral de 500m estaba hardcodeado en src/lib/geo.ts. No se
podía ajustar a 10m, 100m, 5km, etc. según el tipo de siniestro o zona.

FIX:
- migrations/206_geo_threshold_config.sql: nueva tabla system_settings
  (key/value) con seed geo_threshold_meters = 500.
- src/services/settings.ts: servicios getSystemSetting y
  getGeoThresholdMeters() con cache en memoria (1 min) + invalidación.
- src/app/api/settings/geo-threshold/route.ts:
  - GET: devuelve el umbral actual
  - POST: actualiza el valor en system_settings
- src/components/inspection/geo-capture.tsx:
  - Lee el umbral desde /api/settings/geo-threshold
  - Usa ese umbral en validateGeoProximity
  - Muestra el threshold en la validación
- src/app/dashboard/configuracion/GeoThresholdSetting.tsx: panel en la
  pestaña General para editar el umbral (metros) con guardado.
- src/app/dashboard/configuracion/page.tsx: integrado GeoThresholdSetting
  en el tab General.

Además, se confirma que GeoCapture SIEMPRE guarda el mapa de la ubicación
 capturada como evidencia, tanto en inspecciones presenciales como
remotas, independientemente de si el estado es verified o out_of_range.
Cuando es out_of_range en remoto, también guarda el mapa de la dirección
 declarada como evidencia de discrepancia.

Generado con [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
