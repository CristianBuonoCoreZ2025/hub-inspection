feat: liquidation_number clickable → redirige al siniestro

Desde cualquier lugar donde aparezca el número de liquidación (L-000000141),
al hacer click redirige al siniestro (/dashboard/claims/[id]).

Patrón aplicado: Link de next/link con onClick stopPropagation
(para no triggerear el click de la fila/tarjeta contenedora).

Ubicaciones actualizadas:

1. src/app/dashboard/inspecciones/[id]/page.tsx (línea 514)
   - Panel "Datos del Siniestro" → N° Interno

2. src/app/dashboard/inspecciones/page.tsx (línea 200)
   - Tabla de inspecciones → columna Liquidación
   - Usa session.claim_id (SessionClaim no tiene id)

3. src/app/dashboard/informes/page.tsx (línea 542)
   - Tabla de informes → columna Liquidación

4. src/app/dashboard/gestiones/page.tsx (línea 271)
   - Tarjeta de gestión → Siniestro
   - stopPropagation para no navegar a la gestión

5. src/app/dashboard/claims/page.tsx (línea 2158)
   - Tabla de claims → columna Liquidación
   - stopPropagation (la fila completa navega con ?edit=1)

6. src/app/dashboard/operaciones/reabrir/page.tsx (líneas 131, 222)
   - Tabla de siniestros cerrados → columna Liquidación
   - Tabla de siniestros reabiertos → columna Liquidación

7. src/app/dashboard/operaciones/inhabilitar/page.tsx (líneas 138, 233)
   - Tabla de siniestros activos → columna Liquidación
   - Tabla de siniestros inhabilitados → columna Liquidación

No se modificó:
- src/app/inspection/[token]/page.tsx (magic link público)
  → El cliente no tiene acceso al dashboard, se mantiene como texto
- src/app/dashboard/mis-casos/page.tsx
  → La tarjeta completa ya es un Link al siniestro
- src/components/layout/top-bar.tsx
  → Ya es clickable
- src/app/dashboard/agenda/page.tsx
  → Ya es clickable (usa query param ?liquidation=)

Generado con [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
