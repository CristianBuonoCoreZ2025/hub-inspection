import {
  FileCheck,
  Search,
  Send,
  ShieldCheck,
} from "lucide-react";

/*
 * Iconos para el toolbar de siniestros.
 * Lucide profesionales, estilo Apple SF Symbols:
 *   - Liquidación: documento con check (resolución completada)
 *   - Inspección: lupa (revisión visual)
 *   - Despacho: avión de envío (despacho rápido)
 *   - Auditoría: escudo con check (verificación/validación)
 */
export const LiquidacionIcon = FileCheck;
export const InspeccionIcon = Search;
export const DespachoIcon = Send;
export const AuditoriaIcon = ShieldCheck;
